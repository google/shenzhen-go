package internal

// Internal is used to shared constants between the websocket proxy in the frontend and the
// one that lives on the server side. It should not (and cannot) be imported by third parties.
